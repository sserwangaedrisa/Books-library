{
  "name": "books-library",
  "version": "1.0.0",
  "description": "Javascript project that allows the user to add and remove the books from the website",
  "main": "style.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "hint": "^2.0.0"
  }
}
